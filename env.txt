# Backend URL - Update this with your machine's IP address
# For local development on same machine, use localhost
# For external access, use your machine's IP (run: ipconfig getifaddr en0)
REACT_APP_BACKEND_URL=http://localhost:8001

# Alternative: If accessing from another device on same network, uncomment and update IP:
# REACT_APP_BACKEND_URL=http://YOUR_MACHINE_IP:8001
# Example: REACT_APP_BACKEND_URL=http://192.168.1.100:8001
